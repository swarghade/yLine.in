export default function ({ $axios, redirect }) {
/*
  $axios.onResponse(
    (res) => res ,
    error => {
    if(error.response)
      Promise.reject(error.response)
    else
      Promise.reject(error)
  })
*/
}